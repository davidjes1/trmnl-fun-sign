<style>
  /* ===== Fun Sign Styles ===== */

  .fun-sign {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    height: 100%;
    width: 100%;
    box-sizing: border-box;
    overflow: hidden;
  }

  .fun-sign__content {
    display: flex;
    flex-direction: column;
    justify-content: center;
    flex: 1;
    width: 100%;
    padding: 8px;
    box-sizing: border-box;
  }

  .fun-sign__message {
    word-wrap: break-word;
    overflow-wrap: break-word;
    hyphens: auto;
  }

  .fun-sign__subtitle {
    margin-top: 12px;
    opacity: 0.7;
  }

  .fun-sign__empty {
    opacity: 0.5;
    font-style: italic;
  }

  /* --- Text Alignment --- */
  .align--center { text-align: center; }
  .align--left { text-align: left; }
  .align--right { text-align: right; }

  /* --- Font Styles --- */
  .font--classic .fun-sign__message {
    font-size: 36px;
    line-height: 1.3;
    font-weight: 500;
  }
  .font--classic .fun-sign__subtitle {
    font-size: 18px;
  }

  .font--bold .fun-sign__message {
    font-size: 52px;
    line-height: 1.2;
    font-weight: 900;
    letter-spacing: -1px;
  }
  .font--bold .fun-sign__subtitle {
    font-size: 22px;
    font-weight: 600;
  }

  .font--compact .fun-sign__message {
    font-size: 22px;
    line-height: 1.4;
    font-weight: 400;
  }
  .font--compact .fun-sign__subtitle {
    font-size: 14px;
  }

  .font--marquee .fun-sign__message {
    font-size: 44px;
    line-height: 1.2;
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: 4px;
  }
  .font--marquee .fun-sign__subtitle {
    font-size: 16px;
    text-transform: uppercase;
    letter-spacing: 2px;
  }

  /* --- Border Styles --- */
  .border--none {
    border: none;
  }

  .border--simple {
    border: 2px solid #000;
  }

  .border--double {
    border: 6px double #000;
  }

  .border--dashed {
    border: 2px dashed #000;
  }

  .border--thick {
    border: 5px solid #000;
  }

  /* --- Sign Themes --- */

  /* Standard: clean, straightforward */
  .theme--standard {
    padding: 16px;
  }

  /* Notice Board: header bar at top */
  .theme--notice_board {
    padding: 0;
  }
  .theme--notice_board .fun-sign__header {
    display: flex;
    align-items: center;
    justify-content: center;
    background: #000;
    color: #fff;
    padding: 8px 16px;
    font-size: 14px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 2px;
    width: 100%;
    box-sizing: border-box;
  }
  .theme--notice_board .fun-sign__content {
    padding: 16px;
  }

  /* Minimalist: lots of whitespace */
  .theme--minimalist {
    padding: 32px;
  }
  .theme--minimalist .fun-sign__message {
    font-weight: 300;
  }
  .theme--minimalist .fun-sign__subtitle {
    opacity: 0.4;
  }

  /* Retro: decorative double border with inner padding */
  .theme--retro {
    padding: 8px;
  }
  .theme--retro .fun-sign__inner {
    border: 3px solid #000;
    padding: 4px;
    height: 100%;
    box-sizing: border-box;
    display: flex;
    flex-direction: column;
  }
  .theme--retro .fun-sign__inner-inner {
    border: 1px solid #000;
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 12px;
    box-sizing: border-box;
  }

  /* --- Layout-specific size overrides --- */
  .layout--half_horizontal .font--classic .fun-sign__message { font-size: 28px; }
  .layout--half_horizontal .font--bold .fun-sign__message { font-size: 38px; }
  .layout--half_horizontal .font--compact .fun-sign__message { font-size: 18px; }
  .layout--half_horizontal .font--marquee .fun-sign__message { font-size: 32px; }
  .layout--half_horizontal .fun-sign__subtitle { font-size: 14px; }

  .layout--half_vertical .font--classic .fun-sign__message { font-size: 28px; }
  .layout--half_vertical .font--bold .fun-sign__message { font-size: 36px; }
  .layout--half_vertical .font--compact .fun-sign__message { font-size: 18px; }
  .layout--half_vertical .font--marquee .fun-sign__message { font-size: 32px; }

  .layout--quadrant .font--classic .fun-sign__message { font-size: 22px; }
  .layout--quadrant .font--bold .fun-sign__message { font-size: 28px; }
  .layout--quadrant .font--compact .fun-sign__message { font-size: 16px; }
  .layout--quadrant .font--marquee .fun-sign__message { font-size: 24px; letter-spacing: 2px; }
  .layout--quadrant .fun-sign__subtitle { font-size: 12px; }
  .layout--quadrant .theme--minimalist { padding: 16px; }
  .layout--quadrant .theme--notice_board .fun-sign__header { font-size: 11px; padding: 4px 8px; }
</style>
